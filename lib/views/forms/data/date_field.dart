// this will contain code to show date pickerclas

import 'form_textfield.dart';

class DateFormField extends FormTextField {
  DateFormField({
    required super.label,
    required super.hint,
    required super.validator,
    required super.formValueProvider,
  });
}
